"use client"

/**
 * Role-based redirect hook - disabled since auth is removed
 * System runs without user authentication
 */
export function useRoleRedirect() {
  // No-op - no authentication/authorization
}
